// ----------------------------------------------------------------------------
    // Error boundries
// ----------------------------------------------------------------------------
    // ===> Error boundries are react component.
    // ===> which is catch javaScrpt error in child component tree.
    // ===> You can log errors.
    // ===> Errors boundries catch errors during rendring in lifecycle methods.
    // ===> Errors boundries not catch errors:
                // ---> event handlers
                // ---> asynchronous code (setTimeout,requestAnimationFrame callbacks)
    // Server-side rendring
    // Errors thrown in error boundary itself (rather then its children)


    
    